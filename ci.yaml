name: Go Test

on:
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "Starting building ..."
      - name: Test
        run: echo "Start testing ..."
  archive:
    runs-on: ubuntu-latest
    steps:
      - name: Archive
        run: tar ...